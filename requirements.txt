fastapi==0.68.0
fastjsonschema==2.15.0  # Updated to a newer version
h5py==3.4.0  # Updated to a newer version
keras==2.6.0
opencv-python-headless==4.5.3.56  # Adjusted version
tensorflow==2.6.0  # Updated to a compatible version
tensorflow-estimator==2.6.0  # Updated to a compatible version
tensorflow-io-gcs-filesystem==0.21.0  # Adjusted version
urllib3==1.26.7  # Adjusted to a version that exists
uvicorn==0.15.0  # Adjusted to a version that exists
typing-extensions==3.10.0.2  # Specify a version that might resolve the issue
